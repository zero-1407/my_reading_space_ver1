# 깃허브 명령어 모음

> ⚠️ **이 파일에는 토큰을 적지 마세요.**
> 이 파일은 저장소에 함께 올라갑니다. 여기 적으면 공개 저장소에 영구히 남습니다.
> 토큰은 윈도우 자격 증명 관리자가 알아서 기억하니 따로 적어둘 필요가 없습니다.

작업 폴더 : `C:\Users\chuns\Desktop\프로젝트`
저장소    : https://github.com/zero-1407/my_reading_space_ver1

> 클로드 코드 프롬프트에서는 앞에 `!` 를 붙이면 바로 실행됩니다.
> 예 : `! git status`

---

## 0. 지금 해야 하는 것 — 첫 푸시

여기까지는 이미 끝났습니다. (설치 · init · add · commit · remote 연결)
**남은 건 이 한 줄뿐입니다.**

```powershell
git push -u origin main
```

처음 실행하면 로그인 창이 뜹니다. 깃허브 계정으로 승인해 주세요.

---

## 1. 앞으로 매번 쓰는 세 줄

작업하고 나서 이 순서로만 하면 됩니다.

```powershell
git add .
git commit -m "무엇을 바꿨는지 한 줄"
git push
```

첫 푸시 이후로는 `-u origin main` 안 붙여도 됩니다.

### 커밋 메시지 예시

```powershell
git commit -m "재즈바 추가"
git commit -m "기차 시각표와 역무원"
git commit -m "물수제비 캐릭터 앞에서 던지도록 수정"
```

---

## 2. 상태 확인

```powershell
git status              # 무엇이 바뀌었나
git status --short      # 짧게
git log --oneline -10   # 최근 커밋 열 개
git diff                # 아직 add 안 한 변경 내용
git diff --staged       # add 한 변경 내용
git remote -v           # 원격 주소 확인
```

---

## 3. 되돌리기

```powershell
# 파일 하나를 마지막 커밋 상태로 되돌리기
git restore prototype/js/game.js

# add 를 취소 (파일 내용은 그대로)
git restore --staged prototype/js/game.js

# 방금 만든 커밋의 메시지만 고치기 (아직 push 안 했을 때)
git commit --amend -m "고친 메시지"

# 방금 커밋을 취소하고 변경 내용은 남기기 (아직 push 안 했을 때)
git reset --soft HEAD~1
```

> `git reset --hard` 는 작업한 내용이 사라집니다. 쓰기 전에 꼭 물어보세요.

---

## 4. 인증이 막힐 때

깃허브는 **비밀번호를 받지 않습니다.** 토큰이 필요합니다.

1. github.com → 우측 상단 프로필 → **Settings**
2. 맨 아래 **Developer settings**
3. **Personal access tokens → Tokens (classic)**
4. **Generate new token (classic)**
5. Note 에 아무 이름, Expiration 은 90일 정도
6. 체크박스에서 **`repo`** 만 체크
7. **Generate token** → 나온 문자열을 복사 (한 번만 보입니다)

푸시할 때 물어보면
- Username : `zero-1407`
- Password : **복사한 토큰 붙여넣기**

한 번 넣으면 윈도우가 기억합니다.

### 저장된 인증을 지우고 다시 넣고 싶을 때

```powershell
# 제어판 → 자격 증명 관리자 → Windows 자격 증명 → git:https://github.com 삭제
cmdkey /list | findstr github
```

---

## 5. 자주 나는 오류

### `fatal: could not read Username`
로그인 창을 못 띄운 것입니다. 터미널에서 직접 `git push` 를 실행하세요.

### `Updates were rejected because the remote contains work that you do not have`
깃허브 쪽에 내가 모르는 커밋이 있습니다 (보통 README 자동 생성).

```powershell
git pull origin main --allow-unrelated-histories
git push
```

### `LF will be replaced by CRLF`
경고일 뿐입니다. 무시해도 됩니다. 보기 싫으면:

```powershell
git config --global core.autocrlf true
```

### `git` 을 못 찾는다고 할 때
PATH 를 다시 잡아 주세요.

```powershell
$env:Path = "$env:ProgramFiles\Git\cmd;$env:Path"
```

새 창을 열면 대개 저절로 잡힙니다.

---

## 6. 배포 (Render)

푸시하면 Render 가 **자동으로 다시 배포**합니다. 따로 할 게 없습니다.

처음 한 번만:

1. [render.com](https://render.com) 로그인 (깃허브 계정으로)
2. **New → Blueprint**
3. `my_reading_space_ver1` 저장소 선택 → **Apply**
   (`render.yaml` 이 있어서 설정은 안 건드려도 됩니다)
4. 배포된 뒤 **Environment** 탭에서 `KCISA_KEY` 값 입력
   → 전시·도서가 실제 데이터로 바뀝니다. 안 넣어도 신문은 나옵니다.

주소는 `https://my-reading-space.onrender.com` 같은 모양으로 나옵니다.

> 무료 플랜은 15분간 아무도 안 들어오면 잠듭니다.
> 다시 열면 30초쯤 걸려서 깨어납니다. 친구에게 미리 알려주세요.

---

## 7. 절대 올리면 안 되는 것

`.gitignore` 가 막고 있지만 확인용으로 적어둡니다.

- `KCISA_KEY` 같은 **API 키**
- `.env` 파일
- `node_modules/`

키가 실수로 올라갔다면 **그 키는 폐기하고 새로 발급**받으세요.
커밋을 지워도 깃허브 기록에는 남습니다.

---

## 8. 한 번에 복사해서 쓰는 것

```powershell
# 작업 끝나고
git add .
git commit -m "여기에 설명"
git push
```
